# Secure-Client-Server-Communication-System
A Java client-server application that encrypts an image and message using AES-128, transmits the encrypted data over a real WiFi network via TCP sockets, and decrypts it on a receiving device — demonstrating secure communication fundamentals.
